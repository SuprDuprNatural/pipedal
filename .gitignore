#build files
build/
core
#VS Code config files.
# .vscode/  Sort this out later. .gitignore-ing an already tracked file doesn't actually work.
#settings

Testing/

# Local development credentials (never commit).
username.txt

# Local working notes. Records this installation's host names, accounts and
# build/backup paths, so it is kept out of the public repository.
docs/GpioHardwareControlHandover.md

# macOS Finder metadata.
.DS_Store

react/package-lock.json

src/dbus/bluez_adaptor.h
src/dbus/bluez_proxy.h

CMakeFiles
vg.output
